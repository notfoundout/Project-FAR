# FAR-VAL-SELECT-001 — Incomplete changed-file selection

At least one changed path was not covered by a registered input pattern.

The engine must fall back to the complete requested profile. Add or correct manifest input patterns only after confirming which checks the path can affect. Do not create a broad exclusion merely to restore fast selection.
