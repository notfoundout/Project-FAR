# Presenting Project FAR — Recovery Addendum (2026-08-14)

Status: **NONCANONICAL / RECOVERED CONVERSATION-STATE EVIDENCE**

This addendum supplements `.far/inbox/presenting-project-far.md` after the user supplied the ChatGPT shared-conversation URL:

`https://chatgpt.com/share/6a7f34f6-6888-83ea-b41f-7c79a745efc7`

The public web fetch available to this recovery process did **not** expose the shared conversation body, so this file is **not** a verbatim scrape of that URL. However, searching retained prior-conversation records after receiving the link recovered additional assistant-authored state records from the same `Presenting Project FAR` conversation that were absent from the first reconstruction.

Do not upgrade any item below to verbatim transcript unless the original conversation/export supplies the exact message text. These records materially improve the live-state reconstruction but remain `RECONSTRUCTED-HIGH` unless otherwise stated.

---

## Newly recovered state records

### GPT TURN 23 — direct instantiation

`RECONSTRUCTED-HIGH`

Retained prior-conversation record:

- “Direct instantiation” was defined as **specialization plus source-internal definitional expansion**.
- The boundary was represented as `DI1–DI6`.
- Analyst-supplied primitive structure was excluded.
- Cross-source assembly was excluded from direct instantiation.

This strengthens the existing Turn 23 reconstruction: `DI1–DI6` existed as an explicit direct-instantiation boundary, not merely an informal restriction.

### GPT TURN 24 — precondition stop

`RECONSTRUCTED-HIGH`

Retained prior-conversation record:

- If `S1` / `S2` were non-READY, the `𝔈₀` shared-content fragments became **UNTESTABLE**.
- No substitute domain/fragment was permitted.
- The correct outcome was a **PRECONDITION STOP** / domain-instantiation failure rather than an invented replacement.

This is important for queue reconstruction: `𝔈₀` and the S-target readiness conditions were gating structure, not optional context.

### GPT TURN 25 — S1 and negative source adjudication

`RECONSTRUCTED-HIGH`

Retained prior-conversation record:

- `S1` remained **OPEN** under `SR-B2 v1` until a finite retrieval procedure was exhausted.
- Strong negative-looking evidence by itself could not produce `SOURCE-FAIL` before the registered search was complete.

This confirms that the negative source result was procedural/exhaustion-bounded rather than a free-form judgment.

### GPT TURN 26 — SR-B2 v2

`RECONSTRUCTED-HIGH`

Retained prior-conversation records establish the following operational content for the executable `SR-B2 v2`:

- `SR-B2 v1` was retired as capability-infeasible.
- `SR-B2 v2` used **six fixed queries per OPEN target**.
- Each query retrieved **10 results**.
- Up to **five `NM-v1` near-matches** could receive follow-up examination.
- Each such near-match allowed **two title/author follow-up queries**.
- A positive `EXACT` / `DIRECT` result could permit an early positive stop.
- A negative `SOURCE-FAIL` required exhaustion of **all prescribed invocations**.
- `S1` was to rerun from query 1 under the repaired protocol.

This materially upgrades the prior reconstruction of Turn 26 from “an executable v2 was frozen” to a recoverable finite search procedure.

### GPT TURN 27 — NM-v1 and K-P1

`RECONSTRUCTED-HIGH`

Retained prior-conversation record:

- `NM-v1` was deliberately broad at the retrieval stage: same **formal family**, **structural role**, or **provenance** could qualify something as a near-match for inspection.
- `K-P1` / the direct-instantiation eligibility test remained strict.
- A focused-LK paper could enter the `N1`/near-match pool while still failing final eligibility.

This confirms the two-stage design: broad recall for candidate retrieval, strict precision for admissibility.

### GPT TURN 28 — search exhaustion

`RECONSTRUCTED-HIGH`

The first reconstruction already recorded that all six queries were mandatory. The recovered Turn 26 procedure now supplies the missing operational context: this rule prevented selective query execution from manufacturing a negative source result.

### GPT TURN 32 — FDI-v1

`RECONSTRUCTED-HIGH`

Retained prior-conversation records establish more of `FDI-v1` than the first reconstruction captured.

`FDI-v1` was a **five-condition frontier-DI certificate for frozen S1**. Recovered requirements include:

- source-defined **partial derivations**;
- an **open-leaf frontier** and its multiplicity/structure;
- **one-step source-induced evolution**;
- **frontier-quotient well-definedness**;
- partial derivations alone were explicitly **insufficient**.

The available retained record does not safely map every recovered phrase to exact clause labels `FDI1`–`FDI5`; do not invent that mapping.

Turn 32 also participated in the later-withdrawn `DI3` argument, as established verbatim by Claude Turn 33.

### CLAUDE TURN 33

The existing reconstruction contains the recoverable verbatim excerpt and remains authoritative for that excerpt. No additional verbatim completion of the truncated final sentence was recovered here.

### GPT TURN 34 — final S1 adjudication

`RECONSTRUCTED-HIGH`

Retained prior-conversation records recover substantially more of Turn 34:

- `FDI4` was adjudicated satisfied by `DI2`.
- `CDE-v1` was fixed.
- `CDE-v1` allowed **canonical definitional expansions from source-defined structure with no free methodological parameters**.
- **Liang–Miller member 2** was classified `DIRECT` under the relevant direct-instantiation/frontier certificate machinery (`DI1` plus the `FDI1–FDI5` structure in the retained summary).
- `S1` was adjudicated **READY** under the frozen internal protocol.
- `c0` survived.
- The remaining action was recorded as **`S6 → S7`**.

This upgrades the previous Turn 34 reconstruction from a bare S1 state transition to a partial recovery of the adjudication route.

---

## Miller / Liang–Miller source identification

`SOURCE-IDENTIFICATION-HIGH`, not transcript wording.

Independent source lookup identifies two highly relevant primary records matching the dialogue references:

1. **Dale Miller, “A Survey of the Proof-Theoretic Foundations of Logic Programming,” arXiv:2109.01483 (2021).** This source contains the discussion of sequent-calculus proof search having roughly “1000 choices” and describes the low-level proof form that motivated focusing. This is the strong candidate for the “Miller ‘1000 choices’ / proofs are formless” source referenced in Claude Turn 33.

2. **Chuck Liang and Dale Miller, “Focusing and Polarization in Intuitionistic Logic,” arXiv:0708.2252 (2007).** This is a primary Liang–Miller focusing paper and is a strong candidate for the “Liang–Miller member 2” reference in the retained Turn 34 state.

Do not collapse these two sources without the original candidate ledger: the retained dialogue may have used them as distinct members of a source/candidate set.

---

## Revised recovery classification

The first recovery ledger should be revised as follows when the live-state compiler is rerun:

### Improved from NOT_RECOVERED / fact-without-definition to RECOVERED_PARTIALLY

- `SR-B2 v2`: finite search procedure substantially recovered.
- `NM-v1`: broad retrieval criterion substantially recovered.
- `K-P1`: strict final eligibility/direct-instantiation role partially recovered.
- `FDI-v1`: five-condition frontier-DI role and several required components recovered.
- `CDE-v1`: canonical expansion/no-free-methodological-parameter rule partially recovered.
- `FDI1–FDI5`: existence and role recovered; exact clause-by-clause mapping still incomplete.
- Miller source: likely primary source identified.
- Liang–Miller candidate source: likely primary source identified.
- Turn 24: precondition-stop semantics recovered.
- Turn 34: adjudication route partially recovered.

### Still NOT_RECOVERED EXACTLY

- exact text of `T1–T8`;
- exact `𝔈₀` definition;
- exact frozen `S1` statement;
- exact full `DI1–DI6` text;
- exact full `FDI1–FDI5` clause text;
- exact full `CDE-v1` definition;
- exact Turn 33 truncated ending;
- exact missing Turns 1, 6–11, 14, 16, 18–22, 24 message bodies (Turn 24 state is now recoverable, but not its verbatim body).

---

## Consequence for the current queue

These recovered records reduce the missing-evidence set but do **not** yet authorize restarting Presenting FAR autonomously, because the exact frozen `𝔈₀`, `T1–T8`, and frozen `S1` statement remain unavailable.

They do establish that the previous claim “the only occurrence of any Presenting FAR protocol identifier anywhere is inside the reconstruction file itself” was true only for repository/local-file search, not for all recoverable conversation-state evidence. The live reconstruction should ingest this addendum before its next freeze/version.

Do not amend `continuation-freeze-v1.md` in place. Any updated live theory state based on this addendum must be a successor research-state freeze.
