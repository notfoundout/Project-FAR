# Persistent evidence-store staging

This tracked directory is the stable GitHub Actions upload root for FAR persistent evidence-store validation artifacts.

Runtime SQLite databases, source evidence, and immutable blob directories are generated during validation and are not committed.
