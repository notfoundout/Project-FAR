# FAR trace-ingestion evidence

This directory is the repository-recognized staging root for CI-generated trace-ingestion evidence.

The `FAR Trace Ingestion` workflow writes transient `trace.json` and `decision-package.json` files here during execution and uploads them as a short-lived workflow artifact. Generated files are not committed.
