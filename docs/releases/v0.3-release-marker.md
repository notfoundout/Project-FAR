# v0.3 Release Marker

## Status

Repository release marker.

## Milestone

Project FAR v0.3 marks the first verified automation checkpoint.

The repository has:

- theorem metadata;
- verification scripts;
- dependency checks;
- registry checks;
- notation checks;
- circularity checks;
- GitHub Actions verification;
- a passing CI run after verifier correction.

## Note

This file is not a Git tag and not a GitHub Release object. It records the verified v0.3 checkpoint inside the repository because the currently available connector actions do not expose tag or release creation.

A real Git tag should point to the latest green commit after the v0.3 verifier passed.
