# PCA-W5 Execution, Observation, Discovery, and Replication

Status: **Accepted at the registered finite-explicit scope**

## Observation

The exact-rational verifier accepts the frozen frontier and zero-tolerance controls. The randomized candidate has expected loss `1/2`; the exact candidate has loss `0`; a deterministic one-sided candidate has expected loss `1/2`. With cost vectors `(1,3)`, `(3,1)`, and `(4,4)`, the first two candidates are incomparable Pareto minima, the third is dominated, and there is no least element. Under tolerance zero in the registered positive-mass control, only the exact decoder remains feasible.

Expected and maximum aggregation are observably different: the one-sided decoder has expected loss `1/2` but maximum loss `1`. Mutation tests reject false probability, reference, metric/loss, tolerance, cost coverage, Pareto, least-element, exact-recovery, explicit-status, freeze, and manifest claims. A zero-mass mutation confirms that zero expected aggregate implies exactness only on the positive-mass reference support; maximum aggregation continues to inspect every listed case.

## Discovery

Approximation is contract-relative to the metric, reference/aggregation rule, loss, and tolerance. Implementation minimality is separately relative to a candidate universe and cost preorder. Pareto-minimal and least are not interchangeable. For expected aggregation, zero loss recovers exact behavior on the positive-mass reference support; for maximum aggregation it recovers exact behavior on every listed case. Full-domain exactness under expected aggregation therefore requires positive mass on every listed case or an additional independent condition.

## Replication and acceptance

The schema validator, semantic verifier, conformance controls, independent mutation tests, campaign manifest hashes, and bounded Lean statements constitute internal computational/formal replication. The campaign checker independently fixes the required checked-record/artifact set and binds both the semantic verifier and itself, so removing manifest entries cannot create a vacuous pass. Checked evidence also requires explicit behavior/representation tables and a frozen contract.

They establish the encoded result only. No external independent review, novelty, domain completeness, universal cost, or empirical benefit is promoted.

Terminal W5 verdict: **PROVED at the frozen finite-explicit operational scope; complete with boundaries.**
