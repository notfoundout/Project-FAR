# PCA-W5 Execution, Observation, Discovery, and Replication

Status: **Accepted at the registered finite-explicit scope**

## Observation

The exact-rational verifier accepts the frozen frontier and zero-tolerance controls. The randomized candidate has expected loss `1/2`; the exact candidate has loss `0`; a deterministic one-sided candidate has expected loss `1/2`. With cost vectors `(1,3)`, `(3,1)`, and `(4,4)`, the first two candidates are incomparable Pareto minima, the third is dominated, and there is no least element. Under tolerance zero, only the exact decoder remains feasible.

Expected and maximum aggregation are observably different: the one-sided decoder has expected loss `1/2` but maximum loss `1`. Mutation tests reject false probability, reference, metric/loss, tolerance, cost coverage, Pareto, least-element, exact-recovery, and freeze claims.

## Discovery

Approximation is contract-relative to the metric, reference/aggregation rule, loss, and tolerance. Implementation minimality is separately relative to a candidate universe and cost preorder. Pareto-minimal and least are not interchangeable. Zero loss recovers exact behavior only under the declared separating loss and full finite case evaluation.

## Replication and acceptance

The schema validator, semantic verifier, conformance controls, independent mutation tests, campaign manifest hashes, and bounded Lean statements constitute internal computational/formal replication. They establish the encoded result only. No external independent review, novelty, domain completeness, universal cost, or empirical benefit is promoted.

Terminal W5 verdict: **PROVED at the frozen finite-explicit operational scope; complete with boundaries.**
