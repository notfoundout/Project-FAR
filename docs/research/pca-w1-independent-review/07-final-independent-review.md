# Project FAR Core Theory v1.1 — Independent Clean-Room Review

Review program: `PCA-W1-INDEPENDENT-REVIEW`  
Review target commit: `14105775daf3c5713b134a728db2e1e53673af97`  
Verified target tree: `68f058199b7c94b707fd5fe978f1ef59d695ab00`  
Theory identity: `PROJECT-FAR-CORE-THEORY-1.1`  
Designated evidence branch: `research/pca-w1-core-v1.1-independent-review`  
Review date: 2026-08-28  
Final state: **TERMINAL**

## 1. Final verdict

**Final truth verdict for the exact governed 14-claim bundle: `PROVED`.**

| Verdict | Count |
|---|---:|
| PROVED | 14 |
| REFUTED | 0 |
| OPEN | 0 |
| UNDERDETERMINED | 0 |
| NOT APPLICABLE | 0 |

This verdict is deliberately narrow.  It proves the claims only under their
declared Set-based, exact, contract-relative, transformation-relative, or
application-bounded scopes.  It does not prove a universal reasoning
architecture, global primitive basis, contract-free minimum, computationally
efficient quotient, measurable/statistical generalization, or novelty/priority.

The frozen pre-unblinding verdict was `UNDERDETERMINED`: twelve claims were
`PROVED`, while FAR-CORE-013 and FAR-CORE-014 lacked their application definitions
and evidence.  Controlled unblinding supplied exactly those missing premises.
Both were then reconstructed and checked, changing them to `PROVED` without
altering the frozen record.

## 2. Independence and identity

The reviewer is the OpenAI Codex agent assigned to this isolated workstream,
described by the runtime as a GPT-5-based model; no more precise model/build
identifier was exposed.  At review start the reviewer had no prior Project FAR
content exposure.  The task envelope exposed only repository identity, immutable
target/hash assertions, theory ID, branch, protocol path, prohibited-source list,
and procedural requirements.  There was no financial, authorship, governance,
or outcome incentive and no communication with a prior Project FAR reviewer.

The first and only initial substantive repository read was
`docs/research/pca-w1-independent-review-protocol-v1.0.md` from the designated
branch.  Staged-access rules were obeyed.  The complete exposure/environment
record is the first committed review artifact.

Independence outcome: **PASS**.

## 3. Integrity verification

GitHub's commit object for the immutable target identified:

- commit `14105775daf3c5713b134a728db2e1e53673af97`; and
- tree `68f058199b7c94b707fd5fe978f1ef59d695ab00`.

Both match the protocol assertions.  Exact returned target-file bytes produced:

| Artifact | SHA-256 | Protocol expected | Result |
|---|---|---|---|
| `theory/theorems/Project-FAR-Theory-Closure-v1.1.md` | `91513dce21273364ef8ad24ebd1102e3b5957b513bbd5bc429f2b10917fa8239` | same | MATCH |
| `theory/terminal/project-far-core-theory-v1.1.json` | `66372644e5a2fe65c93f7e893e41eae74211934f92827cadcfe28dd81290ab40` | same | MATCH |

The initial commit-object endpoint necessarily exposed target commit metadata,
including a subject/parent summary mentioning the correction PR.  No diff,
parent, history, PR, or correction content was opened before the protocol
allowed it.  This exposure was logged contemporaneously and did not supply a
proof premise.

## 4. Review method actually executed

1. **Stage A — blind reconstruction.**  Reconstructed all FAR-CORE-001–014
   obligations from the permitted v1.1 artifact, wrote explicit proofs,
   countermodels, assumptions, dependency risks, and falsifiers, and committed
   them before opening the machine ledger.
2. **Stage B — ledger reconciliation.**  Read the pinned machine ledger, recorded
   exact matches and mismatches without rewriting the blind record, and locked
   FAR-CORE-014 to only PR #453's stated representation/decoder classes.
3. **Stage C — independent external research.**  Began from an empty corpus,
   issued 54 logged claim-derived queries, admitted 18 primary/authoritative
   sources, retained negative searches and rejection reasons, and mapped direct
   predecessors and hostile boundaries to every claim.
4. **Stage D — irreversible freeze.**  Committed the full claim dossiers and
   terminal verdict at
   `c764a3af9b8f69efc6fd59eae0b2bb658ab40aeb`; separately anchored the document
   hash `e27c99c7ff52f5f95e30e6f163b83cacbfa389d61d6f9408966bf983ce66902e`.
5. **Stage E — controlled unblinding.**  Opened only the protocol-authorized
   v1.0/correction/acceptance/replication/fixture and PR #457 materials, then the
   exact canonical Omega source and PR #453 SSS sources required to decide the
   two underdetermined application claims.  Compared rather than overwrote the
   freeze.

## 5. Final claim-by-claim determinations

| Claim | Exact reviewed scope | Final verdict | Independent basis |
|---|---|---|---|
| FAR-CORE-001 | Any Set-based exact contract; decoder on the reachable representation image | PROVED | `ker(r) subseteq ker(beta)` iff `d(r(x))=beta(x)` is well-defined. |
| FAR-CORE-002 | Any Set-based exact contract; uniqueness on reachable images; cardinal minimum only when finite | PROVED | Every sufficient representation maps uniquely to the observational quotient; equal kernels give an isomorphism. |
| FAR-CORE-003 | Declared typed action/test continuation closure | PROVED | Equality on every continuation test makes the equivalence action-stable, so actions descend. |
| FAR-CORE-004 | Every nontrivial domain; observation contract may vary; universal sufficiency not denied | PROVED | Constant and injective contracts require incompatible least partitions, while identity remains sufficient for both. |
| FAR-CORE-005 | Compatible nested admitted transformation classes | PROVED | If `G subseteq H`, every `H`-invariant property is `G`-invariant. |
| FAR-CORE-006 | Injective underlying-set transport with image decoder and no preservation laws | PROVED | Fixed codes/decoders remain valid while source operations and relations vary arbitrarily; structure is not entailed. |
| FAR-CORE-007 | Faithful reification/tagging regime | PROVED | Equivalent presentations split/combine/reify primitives and change their number/names. |
| FAR-CORE-008 | Finite operator family; tagged tuple domains admitted | PROVED | One tagged dispatcher represents every member and each original is recovered by fixing its tag. |
| FAR-CORE-009 | Proper finite panel inside an open domain; no independent coverage premise | PROVED | Two completions can agree on the panel and disagree at an untested member. |
| FAR-CORE-010 | Fixed `L,J,I` and interpreted models; residue additionally indexed by `Gamma` | PROVED | `Gamma` is absent from `T=intersection Th_L(M_i)` and explicit in `T\Cn_L(Gamma)`; a sentence witness changes only the residue. |
| FAR-CORE-011 | Exact contract with an attainable same-representation/different-behavior omitted-parameter collision | PROVED | The collision violates FAR-CORE-001; syntactic omission or recoverable fields alone do not. |
| FAR-CORE-012 | Reachable absence/Unknown states under a contract that distinguishes them | PROVED | Identifying them creates a behavior collision. |
| FAR-CORE-013 | Canonical FARA Omega definition and semantic consequence composition | PROVED | `Omega(z)=Rep(Class_kappa(z))`, so `r(Omega(z))=(r o Rep o Class_kappa)(z)`; materialization remains operationally useful. |
| FAR-CORE-014 | Only SSS-1/2/3 under PR #453's exact state, transition-representation, and decoder classes | PROVED | Frontier and hyperedge constructions factor derivability; the complete four-member projected-successor decoder class is refuted by two finite MLL witnesses. |

## 6. Hostile tests and computation

### Stage-A finite checks

An independently written enumerator checked:

- 11,141 finite behavior/representation factorization cases;
- 11,141 quotient-minimality comparisons;
- constant/injective universal-minimum obstructions for domain sizes 2–5; and
- 26,122 finite transition/equivalence descent cases.

All encoded cases passed.  These runs are consistency evidence only.

### Stage-E SSS check

A separate post-unblind checker reconstructed unit-free cut-free MLL proof search
for the exact PR-#453 witness fragment.  It enumerated exactly four uniform
monotone successor-truth-set decoders and confirmed:

- the unprovable `S_or` and provable `S_and` have the same projected successor
  truth set `{0,1}`;
- `TERM` and `AND` fail on `S_and`; and
- `OR` and `NONEMPTY` fail on `S_or`.

The captured result is `PASS`.  The general positive/negative proofs, not the
finite run, establish FAR-CORE-014.

### Strongest surviving boundary attacks

- Arbitrary unused codomain labels defeat unnormalized whole-codomain uniqueness;
  the canonical theory explicitly uses the reachable image.
- Current-state observation without continuation closure can fail action descent.
- Singleton domains or a one-partition contract class defeat an overbroad 004.
- Bare injections do not preserve a chosen operation, but declared homomorphisms
  can.
- Fixed signatures/cost constraints can make primitive/operator counts useful.
- Finite data can support probabilistic generalization with capacity assumptions;
  it still does not logically entail an open universal by itself.
- Recoverable omitted fields do not refute sufficiency.
- Stale/corrupt Omega or hidden inputs fall outside canonical derived-view scope.
- Arbitrary binary relations or state-inspecting decoders defeat an overbroad
  SSS impossibility; FAR-CORE-014 excludes them exactly.

## 7. Prior art and novelty

The independent external record includes primary or author-hosted work on
Blackwell comparison, statistical minimal-sufficiency counterexamples,
coalgebraic quotients, Klein invariants, category-theoretic structure-preserving
maps, Morita equivalence, formal learning/generalization, institutions, view
determinacy, complete abstract interpretation, MDP homomorphisms, predictive
state representations, Sheffer bases, SQL-null semantics, materialized views,
and probabilistic testing.

The findings are adverse to any broad novelty claim: most of the mathematical
kernel is elementary or has close, often stronger, domain predecessors.  This
does not refute correctness.  No novelty or priority claim is certified for any
FAR-CORE item.  One exact-phrase search for “Search-State Sufficiency” returned
no relevant scholarly result; that is not novelty proof.

## 8. Controlled-unblind comparison

The preexisting correction audit's three substantive findings were independently
reproduced before it was read:

- FAR-CORE-004 must separate universal sufficiency from simultaneous
  least-informativeness;
- FAR-CORE-010 must separate exact-theory indices from the frame-relative
  residue; and
- Blackwell supplies a decision-problem-uniform order in its classical setup,
  so the former generic characterization was too weak.

The frozen review additionally anticipated the need to lock SSS's representation
and decoder classes.  PR #453 history later disclosed that an earlier unrestricted
binary-relation claim had indeed been false and had been corrected to the exact
bounded classes now proved.

FAR-CORE-013/014 were not called proved before their sources were opened.  Their
final upgrade is explicitly post-unblind and therefore does not erase the
independence boundary.

## 9. Status-label mapping

Every final claim row uses only `PROVED`, `REFUTED`, `OPEN`,
`UNDERDETERMINED`, or `NOT APPLICABLE`.

The v1.1 ledger's `supported_derived` label for FAR-CORE-014 is a historical
governance/evidence-provenance label, not an alternative truth value.  This
review maps the exact bounded proposition to `PROVED` after auditing its source
proofs.  The ledger need not be silently rewritten; governance may retain the
provenance label while recording this independent truth verdict separately.

PR #453 uses `BLOCKED`, `FRAGMENTED`, and `THEORY UNRESOLVED` for a separate
retrieval/common-theory experiment.  Those historical/protocol statuses do not
apply to SSS's displayed mathematical propositions or to any FAR-CORE claim row.

## 10. Unresolved objections and falsifiers

No truth-status objection remains unresolved.  The scope guards in the Stage-D
and Stage-E records remain binding.  A future in-scope contradiction should
reopen only the minimum affected claim.  Particularly decisive falsifiers are:

- an image-level factorization despite a behavior collision, or kernel inclusion
  without a decoder on the image;
- a sufficient representation from which the observational quotient cannot be
  recovered;
- a closed declared test family whose quotient is not action-compatible;
- one representation least for both constant and injective contracts on a
  nontrivial domain;
- a nested-class violation of invariance antitonicity;
- a canonical equal-input/different-Omega lawful semantic consequence; or
- one decoder inside the exact four-member SSS-3 class that computes MLL
  provability from the locked projected relation.

## 11. Minimum repository consequences

No theorem correction is required by this independent review.  The minimum
governance follow-up is assurance promotion, not mathematical rewriting:

1. accept or reject this review through the project's governance process;
2. if accepted, mark `PCA-W1-INDEPENDENT-REVIEW` complete and update only the
   current assurance/status, claim matrix, theorem register, roadmap/next-workstream,
   repository-truth, and export metadata that presently say independent review
   is open;
3. preserve v1.0 byte-for-byte as historical evidence and retain v1.1 as the
   governing theory;
4. record the final FAR-CORE-014 truth verdict separately from its
   `SUPPORTED/DERIVED` provenance label unless governance deliberately versions
   that ledger field; and
5. do not cite CI, repository consistency, internal agreement, or this review's
   finite scripts as substitutes for the proofs.

The scope guards and adverse novelty disposition should accompany any assurance
promotion.  No software behavior change is required.

## 12. Terminal disposition

`PROJECT-FAR-CORE-THEORY-1.1` survives a genuinely isolated hostile review at
the immutable target commit.  All FAR-CORE-001–014 claims are `PROVED` under
their exact scopes.  The terminal negative/positive kernel survives, the
correction audit is independently reproduced, and the result carries no
universal-architecture or novelty claim.
