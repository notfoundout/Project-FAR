# FARA vocabulary sufficiency campaign v1.0

Status: **bounded extension pressure; global sufficiency, minimality, and necessity remain unresolved**

This document is the human-authored interpretation of `FARA-VOC-001`. It is not a second generated report. The sole canonical generated matrix is `theory/evaluation/generated-fara-vocabulary-sufficiency-report.md`, and the machine-readable execution object is `theory/evaluation/fara-vocabulary-sufficiency-v1.0.json`.

## Authority and scope

Repository authority does not designate this campaign as W6. The closest authorized objective is UQ-T7: whether difficult registered systems can be represented without material expressive loss. W0–W5 remain unchanged.

The frozen vocabulary contains Object, Property, Relation, Representation, Interpretation, Investigation, and Reasoning Calculus. Results are relative to the registered executable fixtures, the six preservation dimensions, and exact recovery. They do not establish universal sufficiency or globally primitive extensions.

## Corrected execution design

Each registered family now has its own concrete behavior. The checker executes the full behavior and a reduced version lacking the pressured role. It then computes preservation and recovery from the paired outputs. This replaces the original shared increment/toggle placeholder.

The executable families include probabilistic updating, nonmonotonic retraction, paraconsistent consequence, causal intervention, rule and semantic version changes, ontology and identity operations, provenance-sensitive decisions, partial-order execution, proof identity, higher-order scope, and authority-sensitive rules. Oracle, continuous/hybrid, and embodied/tacit cases remain Unknown because the required external or nonfinite execution is unavailable.

## Result

Deterministic transition, identity-preserving merge, and deletion/constraint relaxation recover exactly. The other finite registered families exhibit bounded extension pressure under the frozen model. Oracle behavior, continuous/hybrid dynamics, and embodied/tacit coupling remain Unknown.

The retained candidates are not promoted to globally primitive concepts. Temporal order remains derivable; oracle relation and external coupling remain external dependencies; the remaining candidates are only boundedly irreducible under this frozen campaign.

## Validation boundary

The validator rebuilds the complete canonical object from executable code and compares it with the stored object. Editing classifications, preservation values, recovery results, obligations, or generated evidence without matching execution therefore fails. The generated report is also checked for freshness.

## Nonclaims

The campaign does not establish universal sufficiency, global minimality, global primitive necessity, necessity from finite ablation, expression from serialization, or global primitive status for any candidate extension.
