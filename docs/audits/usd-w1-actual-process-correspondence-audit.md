# Audit — USD-W1 actual-process correspondence

## Scope integrity

Admission is candidate-independent and distinguishes formal artifacts from independent external observations. No FARA primitive is embedded in the correspondence criteria.

## Evidence audit

The current execution contains formal specifications, proofs, generated traces, and provenance, but no independent measurement model, intervention, or replicated observation linking an actual process to source states.

## Alternative-process audit

Observationally equivalent but internally distinct processes remain admissible alternatives. Matching output or trace labels therefore cannot establish internal-process identity.

## Negative-control audit

Representability-to-correspondence inference and label-identity inference are rejected. Generated traces are classified as implementation evidence, not independent observation.

## Classification audit

`extension_proved` and `proper_subclass_only` would falsely convert a formal result into an empirical identity claim. `countermodel` would overstate the evidence because correspondence is not disproved. The correct terminal outcome is `new_assumption_required`.

## Program audit

This completes terminal execution of all registered `USD-W1-SCOPE-EXT` families. The next decisive workstream is `USD-W2-ALT-VOCAB`. Universal existence, invariance, necessity, minimality, uniqueness, and independent verification remain unresolved.