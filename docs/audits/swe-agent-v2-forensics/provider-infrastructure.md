# Provider and infrastructure analysis

Status: **Research; derived analysis**

The frozen packages record one attempt each, 31 calls, `submitted (exit_cost)`, no remote/network commands, and completed budget-limited patches. They do not record a provider/quota error. Therefore provider or quota failure in these four episodes is **unknown/not evidenced**, not disproven.

The earlier Gemini 2.5/free-tier access episode is separate historical material and was not pooled. The provider access attestation establishes a pre-run probe, not uninterrupted service during every call. The external artifact pipeline retained hashes but not source content in Git, losing repository-only diagnostic opportunity. The post-#400 workflow correction proves that exit 75 could previously become outer success, but the four frozen packages report submitted exit-cost states; no evidence supports reclassifying them as provider failures. Outer workflow success, controller completion and artifact upload remain operational facts only.
