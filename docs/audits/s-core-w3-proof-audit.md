# S_core W3 Global-Witness Proof Audit

## Audit target

This audit covers `SCORE-W3-PROOF-001`, its executable reference, the frozen W0-W2 dependencies, the canonical lemma ledger, theorem target, faithful-representation definition, P8 split, machinery accounting, decomposition, and claim boundaries.

## Audit question

Does W3 construct a complete typed global witness and target-only recovery family for every admitted finite `S_core` source without using hidden source access, changing the frozen target interface, or prematurely claiming `Faithful_split` or a representation theorem?

## Result

**Pass as a project-authored human-checkable W3 proof package.**

The package proves `LEM-SC-017` through `LEM-SC-024` and refutes `OBS-SC-002`, `OBS-SC-007`, `OBS-SC-008`, and `OBS-SC-009` over their registered finite scopes.

## Recovery audit

`RECOVER-FARA-1.0` receives only the target, fixed recovery descriptor, and complete machinery ledger. It does not receive the source, correspondence maps, case identity, evaluator input, network access, or external narrative.

Recovery emits target-local canonical identities. Exact source lexical identifiers are not required because the preservation predicates compare through the typed correspondence package.

The finite recovery stages are schema validation, finite enumeration, grouping, ordered-argument reconstruction, finite graph validation, exact-rational parsing, and canonical sorting. Termination and determinism follow.

## Semantic audit

Material denotations are explicit interpretation records. Source-declared value equivalence is copied and not strengthened. Display labels and identifier spelling do not license semantics. Semantic mutation is rejected by the verifier.

## Coherence audit

One global entity map is restricted to every axis. Shared source items, events, rules, states, and versions reuse one target object. Representations and occurrence records are the only deliberate duplications, and each is typed, linked, and machinery-accounted.

## Machinery audit

The machinery ledger is finite and acyclic. Every `A` and `W` field has a declared producer. Algorithms, schemas, semantic bridges, normalization rules, equivalence rules, composition rules, and cost-bearing helpers are registered. External dependencies are empty.

The executable suite rejects cycles, missing field producers, hidden dependencies, and replacement of the fixed recovery algorithm.

## Composition audit

Components, interfaces, membership, interface links, and cross-component relations are explicit target records. Component views include linked interface members and incident cross-component relations. The proof establishes locality and restriction commutation up to target isomorphism.

## Uniformity audit

The constructor and recovery family have one fixed versioned definition. The proof uses abstract fresh tagged allocation; deterministic digest names in the reference implementation are not mathematical injectivity assumptions. The executable suite confirms source-renaming equivariance on the frozen fixture.

## Obstruction audit

- `OBS-SC-002` is refuted by fixed target-only recovery with no hidden interpreter.
- `OBS-SC-007` is refuted by one terminating equivariant constructor.
- `OBS-SC-008` is refuted by explicit interface preservation and restriction commutation.
- `OBS-SC-009` is refuted by arbitrary-source well-formed witness assembly within the frozen target interface.

These are bounded to finite `S_core`. They do not resolve non-finite, continuous, open-ended, partially observed, or semantically changing `S_IRD` extensions.

## Dependency audit

The proof depends on accepted W0-W2 packages and frozen semantics. It does not depend on `OBS-SC-010`, W5, `Faithful_split`, PB-001 completeness, actual-process correspondence, necessity, minimality, or independent verification.

No premise, source scope, target interface, faithful predicate, P8 boundary, or lemma statement changes.

## Ledger effect

- 24 construction lemmas proved;
- 8 obstruction hypotheses refuted;
- 1 source boundary established;
- 4 obligations open;
- W0-W3 complete;
- W4 active with `OBS-SC-010`;
- W5 open with `ASM-SC-001` through `ASM-SC-003`.

## Gate audit

The formal target, premise/semantics, and faithful-definition gates remain satisfied. The scoped-representation-proof gate remains unsatisfied because the formal negative controls and theorem assembly are open. Mechanized verification and independent proof review remain unsatisfied.

## Claim audit

W3 proves complete construction, recovery, semantics, coherence, machinery, uniformity, composition, and typed witness assembly. It does not prove the global `Nontrivial` conjunct, `Faithful_split`, the `S_core` representation theorem, universality, necessity, or minimality.

## Exact next work

Execute `OBS-SC-010` against NC-01 through NC-10, preserving any countermodel or hidden commitment. Only after that package closes may W5 assemble the strongest finite-core theorem or obstruction.
