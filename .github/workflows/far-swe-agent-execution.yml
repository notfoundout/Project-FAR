name: FAR SWE-agent External Comparison

on:
  workflow_dispatch:
    inputs:
      stage:
        description: Execution stage
        required: true
        type: choice
        options:
          - prepare-environment
          - preflight
          - plan
          - execute
      confirmation:
        description: Use the exact confirmation phrase required by the selected stage
        required: false
        type: string

permissions:
  actions: read
  contents: read
  packages: write

env:
  CASE_DIR: commercial/far-release-assurance/external-validation/swe-agent-v1.0.0-v1.0.1
  SWEBENCH_HARNESS_COMMIT: f7bbbb2ccdf479001d6467c9e34af59e44a840f9
  GHCR_IMAGE: ghcr.io/notfoundout/project-far-swebench-scikit-learn-14125

concurrency:
  group: far-swe-agent-external-comparison
  cancel-in-progress: false

jobs:
  execute-stage:
    runs-on: ubuntu-24.04
    timeout-minutes: 360
    environment: far-swe-agent-execution
    steps:
      - name: Checkout exact selected revision
        uses: actions/checkout@v4
        with:
          persist-credentials: false

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Reclaim runner disk space
        run: |
          sudo rm -rf /usr/local/lib/android /usr/share/dotnet /opt/ghc || true
          docker system prune --all --force || true
          df -h

      - name: Validate manifest and regression tests
        working-directory: ${{ env.CASE_DIR }}
        run: |
          python validate_manifest.py
          python -m unittest -v \
            test_validate_manifest.py \
            test_run_comparison.py \
            test_prepare_environment_contract.py \
            test_execute_controller.py

      - name: Checkout pinned SWE-bench harness
        if: inputs.stage == 'prepare-environment'
        run: |
          git clone --filter=blob:none https://github.com/SWE-bench/SWE-bench.git "$RUNNER_TEMP/swebench-harness"
          git -C "$RUNNER_TEMP/swebench-harness" checkout --detach "$SWEBENCH_HARNESS_COMMIT"
          test "$(git -C "$RUNNER_TEMP/swebench-harness" rev-parse HEAD)" = "$SWEBENCH_HARNESS_COMMIT"
          test -s "$RUNNER_TEMP/swebench-harness/swebench/harness/constants/fixtures/tokio-rs__tokio-6724.Cargo.lock"

      - name: Install pinned SWE-bench harness in editable source mode
        if: inputs.stage == 'prepare-environment'
        run: |
          python -m pip install --upgrade pip
          python -m pip install --editable "$RUNNER_TEMP/swebench-harness"

      - name: Verify SWE-bench import provenance, fixtures, and exact TestSpec interface
        if: inputs.stage == 'prepare-environment'
        env:
          SWEBENCH_HARNESS_DIR: ${{ runner.temp }}/swebench-harness
        run: |
          python - <<'PY'
          import os
          from pathlib import Path
          import swebench
          import swebench.harness.constants.rust as rust
          from swebench.harness.test_spec.test_spec import TestSpec

          expected = Path(os.environ["SWEBENCH_HARNESS_DIR"]).resolve()
          imported = Path(swebench.__file__).resolve()
          rust_file = Path(rust.__file__).resolve()
          fixture = expected / "swebench/harness/constants/fixtures/tokio-rs__tokio-6724.Cargo.lock"
          required = {
              "base_dockerfile", "env_dockerfile", "instance_dockerfile",
              "setup_env_script", "install_repo_script", "eval_script",
              "base_image_key", "env_image_key", "instance_image_key", "platform",
          }
          assert expected in imported.parents, (expected, imported)
          assert expected in rust_file.parents, (expected, rust_file)
          assert fixture.is_file() and fixture.stat().st_size > 0, fixture
          assert required <= set(dir(TestSpec)), sorted(required - set(dir(TestSpec)))
          assert "setup_repo_script" not in dir(TestSpec)
          PY

      - name: Build local SWE-bench environment
        if: inputs.stage == 'prepare-environment'
        working-directory: ${{ env.CASE_DIR }}
        env:
          SWEBENCH_HARNESS_DIR: ${{ runner.temp }}/swebench-harness
        run: python prepare_environment.py

      - name: Publish immutable environment image to GHCR
        if: inputs.stage == 'prepare-environment'
        working-directory: ${{ env.CASE_DIR }}
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          echo "$GH_TOKEN" | docker login ghcr.io -u "${{ github.actor }}" --password-stdin
          LOCAL_IMAGE="$(python -c 'import json; print(json.load(open("execution-output/environment-freeze/environment-lock.json"))["image_key"])')"
          TAG="task-14125-harness-${SWEBENCH_HARNESS_COMMIT::12}"
          docker tag "$LOCAL_IMAGE" "$GHCR_IMAGE:$TAG"
          docker push "$GHCR_IMAGE:$TAG"
          docker pull "$GHCR_IMAGE:$TAG"
          IMMUTABLE="$(docker inspect --format='{{index .RepoDigests 0}}' "$GHCR_IMAGE:$TAG")"
          DIGEST="${IMMUTABLE##*@}"
          test "$IMMUTABLE" = "$GHCR_IMAGE@$DIGEST"
          python finalize_registry_lock.py --reference "$GHCR_IMAGE" --digest "$DIGEST"
          grep -Eq '^[0-9a-f]{64}$' execution-output/environment-freeze/environment-lock.sha256

      - name: Authenticate to GHCR for frozen image pull
        if: inputs.stage == 'preflight' || inputs.stage == 'plan' || inputs.stage == 'execute'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: echo "$GH_TOKEN" | docker login ghcr.io -u "${{ github.actor }}" --password-stdin

      - name: Enforce plan confirmation
        if: inputs.stage == 'plan'
        env:
          CONFIRMATION: ${{ inputs.confirmation }}
        run: |
          test "$CONFIRMATION" = "RUN-FAR-FREE-TIER-COMPARISON" || {
            echo "Plan stage requires RUN-FAR-FREE-TIER-COMPARISON exactly." >&2
            exit 1
          }

      - name: Enforce execution confirmation
        if: inputs.stage == 'execute'
        env:
          CONFIRMATION: ${{ inputs.confirmation }}
        run: |
          test "$CONFIRMATION" = "EXECUTE-FAR-FROZEN-NEXT-RUN" || {
            echo "Execute stage requires EXECUTE-FAR-FROZEN-NEXT-RUN exactly." >&2
            exit 1
          }

      - name: Run frozen-input preflight
        if: inputs.stage == 'preflight'
        working-directory: ${{ env.CASE_DIR }}
        run: python run_comparison.py preflight

      - name: Generate free-tier execution plan
        if: inputs.stage == 'plan'
        working-directory: ${{ env.CASE_DIR }}
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
        run: |
          test -n "$GEMINI_API_KEY" || {
            echo "GEMINI_API_KEY is missing; no model call was started." >&2
            exit 1
          }
          echo "::add-mask::$GEMINI_API_KEY"
          python run_comparison.py plan

      - name: Restore latest validated execution state
        if: inputs.stage == 'execute'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          mkdir -p "$CASE_DIR/execution-output"
          ARTIFACT_ID="$(gh api "repos/$GITHUB_REPOSITORY/actions/artifacts?per_page=100" --jq '[.artifacts[] | select(.expired == false and (.name | startswith("far-swe-agent-execute-")))] | sort_by(.created_at) | reverse | .[0].id // empty')"
          if [ -n "$ARTIFACT_ID" ]; then
            echo "Restoring execution state from artifact $ARTIFACT_ID"
            gh api "repos/$GITHUB_REPOSITORY/actions/artifacts/$ARTIFACT_ID/zip" > "$RUNNER_TEMP/execution-state.zip"
            unzip -o "$RUNNER_TEMP/execution-state.zip" -d "$CASE_DIR/execution-output"
          else
            echo "No prior execution artifact found; starting from the frozen initial state."
          fi

      - name: Resolve next frozen SWE-agent release
        if: inputs.stage == 'execute'
        id: next-run
        working-directory: ${{ env.CASE_DIR }}
        run: |
          python execute_controller.py status > "$RUNNER_TEMP/execution-status.json"
          FULL_COMMIT="$(python -c 'import json,sys; s=json.load(open(sys.argv[1])); r=next((x for x in s["runs"] if x["state"] in {"pending","running","quota_paused","failed_retryable"}), None); print("" if r is None else r["full_commit"])' "$RUNNER_TEMP/execution-status.json")"
          RUN_ID="$(python -c 'import json,sys; s=json.load(open(sys.argv[1])); r=next((x for x in s["runs"] if x["state"] in {"pending","running","quota_paused","failed_retryable"}), None); print("complete" if r is None else r["run_id"])' "$RUNNER_TEMP/execution-status.json")"
          echo "full_commit=$FULL_COMMIT" >> "$GITHUB_OUTPUT"
          echo "run_id=$RUN_ID" >> "$GITHUB_OUTPUT"

      - name: Checkout and install exact SWE-agent release
        if: inputs.stage == 'execute' && steps.next-run.outputs.run_id != 'complete'
        run: |
          git clone --filter=blob:none https://github.com/SWE-agent/SWE-agent.git "$RUNNER_TEMP/swe-agent"
          git -C "$RUNNER_TEMP/swe-agent" checkout --detach "${{ steps.next-run.outputs.full_commit }}"
          test "$(git -C "$RUNNER_TEMP/swe-agent" rev-parse HEAD)" = "${{ steps.next-run.outputs.full_commit }}"
          python -m pip install --upgrade pip
          python -m pip install --editable "$RUNNER_TEMP/swe-agent"

      - name: Parse exact frozen configuration without a model call
        if: inputs.stage == 'execute' && steps.next-run.outputs.run_id != 'complete'
        working-directory: ${{ env.CASE_DIR }}
        run: |
          python - <<'PY'
          import json
          from pathlib import Path

          task = json.loads(Path('environment-freeze/task-record.public.json').read_text())
          lock = json.loads(Path('environment-freeze/environment-lock.json').read_text())
          instance = [{
              'image_name': lock['immutable_image_reference'],
              'problem_statement': task['problem_statement'],
              'id': task['instance_id'],
              'repo_name': 'testbed',
              'base_commit': task['base_commit'],
              'extra_fields': {'outcome_data_accessible': False},
          }]
          Path('/tmp/far-instance.json').write_text(json.dumps(instance))
          PY
          sweagent run-batch \
            --config "$RUNNER_TEMP/swe-agent/config/default.yaml" \
            --config agent-config.yaml \
            --instances.type=file \
            --instances.path=/tmp/far-instance.json \
            --output_dir=/tmp/far-config-parse \
            --num_workers=1 \
            --progress_bar=False \
            --random_delay_multiplier=0 \
            --raise_exceptions=True \
            --redo_existing=False \
            --print_config > /tmp/far-parsed-config.yaml
          python - <<'PY'
          from pathlib import Path
          import yaml
          frozen = yaml.safe_load(Path('agent-config.yaml').read_text())
          parsed = yaml.safe_load(Path('/tmp/far-parsed-config.yaml').read_text())
          assert parsed['agent'] == frozen['agent']
          assert parsed['num_workers'] == 1
          assert parsed['redo_existing'] is False
          assert parsed['instances']['type'] == 'file'
          PY

      - name: Pull and verify frozen environment image
        if: inputs.stage == 'execute' && steps.next-run.outputs.run_id != 'complete'
        working-directory: ${{ env.CASE_DIR }}
        run: |
          IMAGE="$(python -c 'import json; print(json.load(open("environment-freeze/environment-lock.json"))["immutable_image_reference"])')"
          docker pull "$IMAGE"
          docker image inspect "$IMAGE" >/dev/null

      - name: Execute exactly one next frozen run
        if: inputs.stage == 'execute' && steps.next-run.outputs.run_id != 'complete'
        working-directory: ${{ env.CASE_DIR }}
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
        run: |
          test -n "$GEMINI_API_KEY" || {
            echo "GEMINI_API_KEY is missing; no model call was started." >&2
            exit 1
          }
          echo "::add-mask::$GEMINI_API_KEY"
          set +e
          python execute_controller.py execute-next --agent-repo "$RUNNER_TEMP/swe-agent"
          RC=$?
          set -e
          if [ "$RC" -eq 75 ]; then
            echo "Quota pause recorded. Re-run this same execute stage after the provider quota resets." >> "$GITHUB_STEP_SUMMARY"
            exit 0
          fi
          exit "$RC"

      - name: Report completed execution matrix
        if: inputs.stage == 'execute' && steps.next-run.outputs.run_id == 'complete'
        working-directory: ${{ env.CASE_DIR }}
        run: |
          python execute_controller.py status
          echo "All four frozen runs are complete. No additional model call was made." >> "$GITHUB_STEP_SUMMARY"

      - name: Upload non-secret execution artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: far-swe-agent-${{ inputs.stage }}-${{ github.run_id }}
          path: |
            ${{ env.CASE_DIR }}/execution-output/
          if-no-files-found: ignore
          retention-days: 30
