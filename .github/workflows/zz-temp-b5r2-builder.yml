name: TEMP B5R2 bootstrap builder (disabled)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "B5R2 final candidate was published to bootstrap/b5r2-final; this temporary publisher is disabled."
