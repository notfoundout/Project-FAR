name: FAR SWE-agent v2 Setup Smoke

on:
  pull_request:
    paths:
      - '.github/workflows/far-swe-agent-execution-v2.yml'
      - '.github/workflows/far-swe-agent-v2-setup-smoke.yml'
      - 'commercial/far-release-assurance/external-validation/swe-agent-v1.0.0-v1.0.1-gemini-3.1-pro-preview-v2/**'
      - 'tools/check_repository_hygiene.py'
  push:
    branches:
      - main
    paths:
      - '.github/workflows/far-swe-agent-execution-v2.yml'
      - '.github/workflows/far-swe-agent-v2-setup-smoke.yml'
      - 'commercial/far-release-assurance/external-validation/swe-agent-v1.0.0-v1.0.1-gemini-3.1-pro-preview-v2/**'
      - 'tools/check_repository_hygiene.py'

permissions:
  contents: read
  packages: read

env:
  CASE_DIR: commercial/far-release-assurance/external-validation/swe-agent-v1.0.0-v1.0.1-gemini-3.1-pro-preview-v2

jobs:
  preregistration-contract:
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
        with:
          persist-credentials: false

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install parser dependency
        run: python -m pip install PyYAML==6.0.2

      - name: Validate preregistration and workflow contracts
        working-directory: ${{ env.CASE_DIR }}
        run: |
          set +e
          RC=0
          python case_tools.py validate > "$RUNNER_TEMP/v2-preregistration.log" 2>&1 || RC=1
          python -m unittest -v test_case_v2.py test_access_probe_v2.py test_budget_limited_completion_v2.py >> "$RUNNER_TEMP/v2-preregistration.log" 2>&1 || RC=1
          cat "$RUNNER_TEMP/v2-preregistration.log"
          exit "$RC"

      - name: Verify untouched execution and valid access lifecycle
        working-directory: ${{ env.CASE_DIR }}
        run: |
          test ! -e execution-output/execution-state.json
          python - <<'PY'
          import hashlib
          import json
          from pathlib import Path

          manifest = json.loads(Path("manifest.json").read_text())
          status = manifest["status"]
          attestation = Path("access-freeze/provider-access-attestation.json")
          attestation_hash = Path("access-freeze/provider-access-attestation.sha256")
          gate = manifest["provider_access_gate"]

          if status == "preregistered_access_pending":
              assert not attestation.exists()
              assert not attestation_hash.exists()
              assert gate["attestation_path"] is None
              assert gate["attestation_sha256"] is None
              assert gate["verified_at"] is None
          elif status == "execution_inputs_frozen":
              assert attestation.is_file()
              assert attestation_hash.is_file()
              actual = hashlib.sha256(attestation.read_bytes()).hexdigest()
              assert gate["attestation_path"] == str(attestation)
              assert gate["attestation_sha256"] == actual
              assert attestation_hash.read_text().strip() == actual
              assert isinstance(gate["verified_at"], str) and gate["verified_at"]
          else:
              raise AssertionError(f"Unexpected v2 lifecycle status: {status}")
          PY

      - name: Upload preregistration diagnostics
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: far-swe-agent-v2-preregistration-${{ github.run_id }}
          path: ${{ runner.temp }}/v2-preregistration.log
          if-no-files-found: warn
          retention-days: 7

  exact-release-config-contract:
    runs-on: ubuntu-24.04
    timeout-minutes: 30
    strategy:
      fail-fast: false
      matrix:
        include:
          - release: v1.0.0
            commit: 8ed382c1af1a21f63410b9a0cda14759e64b49c0
          - release: v1.0.1
            commit: 6aff2155dd6fb2a8d19069f5c344f85a54f6c2fa
    steps:
      - uses: actions/checkout@v4
        with:
          persist-credentials: false

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Checkout exact SWE-agent release
        run: |
          git clone --filter=blob:none https://github.com/SWE-agent/SWE-agent.git "$RUNNER_TEMP/swe-agent"
          git -C "$RUNNER_TEMP/swe-agent" checkout --detach "${{ matrix.commit }}"
          test "$(git -C "$RUNNER_TEMP/swe-agent" rev-parse HEAD)" = "${{ matrix.commit }}"
          test -s "$RUNNER_TEMP/swe-agent/pyproject.toml"
          test -s "$RUNNER_TEMP/swe-agent/config/default.yaml"
          test -s "$RUNNER_TEMP/swe-agent/sweagent/run/run_batch.py"

      - name: Install exact SWE-agent release
        run: |
          python -m pip install --upgrade pip
          python -m pip install --editable "$RUNNER_TEMP/swe-agent"

      - name: Parse exact v2 configuration without provider access
        working-directory: ${{ env.CASE_DIR }}
        run: |
          set +e
          python validate_agent_config.py \
            --release-default "$RUNNER_TEMP/swe-agent/config/default.yaml" \
            > "$RUNNER_TEMP/v2-config-${{ matrix.release }}.log" 2>&1
          RC=$?
          set -e
          cat "$RUNNER_TEMP/v2-config-${{ matrix.release }}.log"
          exit "$RC"

      - name: Prove parser preserved execution and access evidence
        working-directory: ${{ env.CASE_DIR }}
        run: |
          test ! -e execution-output/execution-state.json
          git diff --exit-code -- manifest.json access-freeze/
          python - <<'PY'
          import hashlib
          import json
          from pathlib import Path

          manifest = json.loads(Path("manifest.json").read_text())
          status = manifest["status"]
          attestation = Path("access-freeze/provider-access-attestation.json")
          attestation_hash = Path("access-freeze/provider-access-attestation.sha256")
          gate = manifest["provider_access_gate"]

          if status == "preregistered_access_pending":
              assert not attestation.exists()
              assert not attestation_hash.exists()
          elif status == "execution_inputs_frozen":
              assert attestation.is_file()
              assert attestation_hash.is_file()
              actual = hashlib.sha256(attestation.read_bytes()).hexdigest()
              assert gate["attestation_sha256"] == actual
              assert attestation_hash.read_text().strip() == actual
          else:
              raise AssertionError(f"Unexpected v2 lifecycle status: {status}")
          PY

      - name: Upload configuration diagnostics
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: far-swe-agent-v2-config-${{ matrix.release }}-${{ github.run_id }}
          path: ${{ runner.temp }}/v2-config-${{ matrix.release }}.log
          if-no-files-found: warn
          retention-days: 7
