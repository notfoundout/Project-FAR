# Reports

`reports/` contains repository assessments, execution histories, and review-cycle summaries.

`reports/` does not define canonical theory.

`reports/` does not resolve research obligations.

`reports/` summarizes repository state based on discovery and execution artifacts.
